# SwiftPM
.build/
.swiftpm/
Package.resolved

# Bundle prodotto
*.app

# DMG distribuibile (va come asset di una Release, non nel repo)
*.dmg

# Asset derivati (rigenerabili da Assets/AppIcon-1024.png via Scripts/make_icon.swift)
Assets/AppIcon-masked.png

# macOS
.DS_Store

# Settings locali di Claude Code (personali, non condivisi)
.claude/settings.local.json

# Repo di riferimento read-only (CodexBar) — non parte del progetto
.reference/

# Xcode user data (se in futuro si apre il package in Xcode)
xcuserdata/
*.xcuserstate
